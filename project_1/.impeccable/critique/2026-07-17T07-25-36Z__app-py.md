---
target: app.py
total_score: 17
p0_count: 0
p1_count: 3
timestamp: 2026-07-17T07-25-36Z
slug: app-py
---
Method: dual-agent (A: 디자인 리뷰 서브에이전트 · B: 검출기/브라우저 증거 서브에이전트)

# Design Health Score — 17/40 (Poor)

| # | Heuristic | Score | 핵심 이슈 |
|---|-----------|:---:|---|
| 1 | 시스템 상태 가시성 | 2 | 생성 중 실시간 스트리밍은 훌륭하나 언어가 노드명(plan_story 등). 현재 단계(words/age/theme)를 입력창 placeholder가 알려주지 않음 |
| 2 | 현실 세계와의 일치 | 2 | 워디의 존댓말은 좋으나 결과가 `**p1.**` 목록, 코드폰트 파일 경로, "검증 실패 N회 재작성" — 개발 로그의 언어 |
| 3 | 사용자 제어와 자유 | 1 | 단계 되돌리기 전무. 유일한 탈출구 "대화 초기화"는 확인 없이 전체 소실 |
| 4 | 일관성과 표준 | 2 | 한국어 UI에 영어 Streamlit 크롬 혼재, 성격 다른 확장 패널 3개가 같은 무게 |
| 5 | 오류 예방 | 1 | age가 `\d+` 파싱("20개월"→20살), 범위 검증 없음. theme는 아무 텍스트나 즉시 전체 생성 발동 — 확인 게이트 없음 |
| 6 | 재인 > 회상 | 1 | 형식 규칙·이미 입력한 값이 지나간 말풍선에만 존재, 상기 장치 없음 |
| 7 | 유연성과 효율 | 2 | 아이별 메모리·주인공 커스텀은 좋으나 예시 칩·재사용 지름길 전무 |
| 8 | 미학과 미니멀리즘 | 1 | wide 레이아웃으로 본문 100ch+, 동화 본문이 UI 텍스트와 동일 크기 — "본문이 주인공" 미달성 |
| 9 | 오류 인식·복구 | 2 | words 거절은 준수하나 생성 후 거절은 세미콜론 나열 + 다음 행동 안내 없음 + stage 무언 리셋 |
| 10 | 도움말과 문서 | 3 | 이용 가이드 카피는 우수. 30줄 텍스트 월 + "위의" 공간 참조가 감점 |

Total 17/40 — Poor 대역(12–19). 골격은 제품 감각이 있으나 제어·예방·표현 레이어가 빈 프로토타입 점수.

# Anti-Patterns Verdict

**LLM 평가**: "AI가 만든 기본 템플릿"이라고 즉시 판정. 정확히는 generic slop이 아니라 "개발자의 데모" — PRODUCT.md 안티 레퍼런스 1번(차가운 개발자 도구)과 일치. 테마 커스터마이징 0 (DESIGN.md 커밋 룰 달성률 0%), 헤더에 "LangGraph 에이전트" 노출, 노드 로그 기본 펼침, 그래프 다이어그램·"LLM 모드: mock" 노출.

**결정적 스캔**: `detect.mjs --json app.py` → 발견 0건(exit 0). 단, 검출기 규칙은 HTML/CSS 지향이라 순수 Python Streamlit 파일에는 실질 적용 불가 — 빈 결과를 "문제 없음"의 근거로 쓸 수 없음(위음성 가능성 명시). 위양성 없음.

**시각 증거**: Playwright/브라우저 도구 부재로 스크린샷·오버레이 수집 건너뜀. 코드 관찰로 기본 Streamlit 테마 적용(커스텀 테마/CSS 0건, `layout="wide"`)은 확실.

# Overall Impression

대화 설계와 카피는 이미 워디인데, 화면은 아직 LangGraph다. 스테이지 플로우·실시간 피드백·온보딩 가이드라는 골격은 제품 감각이 있으므로, 테마 커밋 + 기술 언어 격리 + 완성/거절 순간의 감정 설계 세 가지로 20대 중후반까지 오를 수 있는 상태.

# What's Working

1. **이용 가이드 카피 (app.py:58-85)** — ⭕/❌ 대비 예시, 단어 세트 제안, 주인공 공식. 좋은 입력을 가르치는 온보딩. 첫 방문만 자동 펼침 조건도 정확.
2. **스테이지별 확인 에코 (app.py:297)** — 매 단계가 입력을 반복 확인하고 다음 질문을 즉시 던짐. PRODUCT.md 원칙 2를 대화 층위에서 달성.
3. **목소리 업로드 상태 설계 (app.py:138-187)** — 역할별 진행 표시, nonce로 중복 저장 방지, rerun 너머 결과 전달. 코드베이스에서 가장 성숙한 컴포넌트.

# Priority Issues

- **[P1] 기본 Streamlit 테마 = 안티 레퍼런스 그 자체.** 살구·주황 0%, 회색 대시보드 룩. 브랜드 약속("첫눈에 동화 앱")이 첫 프레임에서 깨짐. Fix: `.streamlit/config.toml` 테마 + 커스텀 CSS로 동화 본문 블록 확대. → /impeccable colorize + typeset
- **[P1] 기술 용어 전면 노출.** "LangGraph 에이전트"(131), 노드 로그 기본 펼침(233), 그래프 expander(204), "LLM 모드: mock"(120). 원칙 3("기술은 무대 뒤에") 정면 위반. Fix: status 라벨을 워디 언어로, 트레이스 접기, 개발자 요소는 플래그 뒤로. → /impeccable clarify + distill
- **[P1] 단계 되돌리기 부재 + 거절 후 막다른 길.** 오타 수정 불가, 생성 후 거절(318) 시 안내 없이 stage 무언 리셋 — "왜요?"라고 답하면 그게 단어 검사에 들어가는 혼란 루프. Fix: "다시/뒤로" 처리, 거절 메시지에 다음 행동 + 대안 단어. → /impeccable harden + clarify
- **[P2] 입력 함정.** `\d+` age 파싱("20개월"→20살), theme 입력 즉시 무확인 풀 생성 — 실수 한 번에 수 분 LLM 대기·비용. Fix: 개월 인식 + 범위 재확인, 생성 전 원클릭 확인 게이트. → /impeccable harden
- **[P2] 완성 순간이 평평하고 자기 비하적.** `**p1.**` 목록, "검증 실패로 N회 재작성"(272), 코드폰트 파일 경로(274). peak가 로그처럼 보임. Fix: 본문 전용 스타일 블록(큰 활자·65ch), "실패" 문구 반전, download_button. → /impeccable delight + typeset

# Persona Red Flags

**Jordan (첫 방문자)**: 가이드 자동 펼침이 입력창을 폴드 아래로 밀어냄. 그래프 다이어그램에 "내가 뭘 잘못 눌렀나". `빨간 사과, 작은 나비`가 6개 단어로 쪼개짐(292). "LLM 모드: mock" 해독 불가.

**Casey (한 손 모바일 부모)**: 모바일에서 사이드바 접힘 → 아이 이름을 영영 설정 못 해 메모리 기능 존재를 모름. 쉼표 구분 5~10개 단어 타이핑이 첫 단계 최대 장벽. 생성 중 status 로그가 화면을 계속 밀어냄.

**Sam (스크린리더)**: 이모지 선행 라벨("렌치 이모지 툴 원…"), 제목 없는 그래프 iframe, 매 입력 rerun으로 포커스 최상단 이탈, 고정 placeholder로 현재 단계 식별 불가, 기본 caption 회색 대비 AA 미달 가능성.

**저녁 9시 피곤한 부모**: 어두운 침실에서 순백 배경 광원 — "노을빛" 약속과 정반대. 생성 후 거절이 뜨면 사실상 이탈. 본문이 작은 글자 + 긴 줄이라 소리 내어 읽기 부적합. 파일 경로는 폰에서 무용.

# Minor Observations

- `st.metric` 배운 단어 N개(114) — 성적표 감각, DESIGN.md "추억처럼" 위반
- 대화 초기화 무확인(122), 빈 이름 시 thread_id 공유(110)
- mermaid CDN 의존(196) — 차단 환경에서 무음 빈 iframe
- 목소리 mp3만 허용(176) — 아이폰 기본 m4a 거절
- 업로드 후 저장 버튼 안 누르면 무음 유실(180)
- 생성 중 취소 수단 없음(315)

# Questions to Consider

1. 왜 나이를 매번 물어보는가? 사이드바 "아이 설정"으로 옮기면 반복 입력이 단어+테마로 줄어든다. 더 밀면 — 고정 3슬롯 상태 기계에 자유 텍스트 채팅이 맞는 형식인가?
2. 완성된 동화가 '책'이라면 왜 말풍선 속 목록인가? 페이지 넘김이 있는 읽기 모드가 제품의 진짜 최종 화면 아닌가?
3. 생성 대기 1~2분을 워디의 중계("구름이랑 나비가 어디서 만날까 고민 중이에요…")로 콘텐츠화할 수 있지 않은가?
